class Custom:
  def __init__(self, position, rotation, scale, mesh, collider):
    self.position = position
    self.rotation = rotation
    self.scale = scale
    self.mesh = mesh
    self.collider = collider
    