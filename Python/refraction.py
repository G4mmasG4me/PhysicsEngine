# refraction stack

refraction_stack = []
# when entering object, add to stack
# when leaving object, remove from stack, and then current object is the last item in stack
# if no item in stack then current refraction is air