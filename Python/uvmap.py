from Tree import TreeNode

def uv_unwrap(object):
  selected = [object.face[0]]
  node = TreeNode(object.face[0])
  # start at face
  while True:
    pass
    # create a list of connecting face that haven't been selected before
    # if connecting faces:
      # select a random face
    # else
    # go back to previous face
    # if at original face and no connecting faces
      # break

